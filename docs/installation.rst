============
Installation
============

At the command line::

    $ easy_install ddlgenerator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ddlgenerator
    $ pip install ddlgenerator